# Web Server Configuration
WEB_PORT=3000
WEB_API_KEY=CHANGE_ME_TO_STRONG_PASSWORD

# Generate a strong API key with:
# openssl rand -base64 32

# Optional: Override config.json values
# NODE_ENV=production

